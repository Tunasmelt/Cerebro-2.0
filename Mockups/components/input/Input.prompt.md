Text field for search, note titles, and settings. Set `mono` when the value is code-like (a chunk ID, file path, API key) rather than prose.

```jsx
<Input label="Vault name" placeholder="My second brain" />
<Input label="Chunk ID" mono defaultValue="chk_9f2a1c" />
```
